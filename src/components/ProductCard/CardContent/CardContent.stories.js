import React from 'react';

import CardContent from './CardContent';

export default {
  component: CardContent,
  title: 'Components/ProductCard/CardContent',
};

const Template = (args) => <CardContent {...args} />;

export const Default = Template.bind({});

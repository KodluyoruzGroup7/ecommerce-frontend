import React from 'react';

import CardImg from './CardImg';

export default {
  component: CardImg,
  title: 'Components/ProductCard/CardImg',
};

const Template = (args) => <CardImg {...args} />;

export const Default = Template.bind({});
